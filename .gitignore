/target
node_modules/
dist/
gen/
.DS_Store
.env
.env.*
*.local

# local-only (scanner infra, never commit — public repo)
scripts/run-scanner.sh

# Python leaves these behind when a script is imported (e.g. testing the
# version bumper). They are build noise and clutter another agent's git status.
__pycache__/
*.pyc

# wrangler's local simulated R2/KV, created by `wrangler r2 object` without --remote
.wrangler/
